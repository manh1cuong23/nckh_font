// import useAuthStore from '../../hooks/useAuthStore';
// import { Navigate, Route } from 'react-router-dom';

// function AuthComposition({ children }) {
//     const user = useAuthStore((state) => state.user);
//     return (
        
//             {
//                 user ? 
//                     children // Nếu đã đăng nhập, render component được chuyển đến
//                   ) : 
//                     <Navigate to="/login"/ > // Nếu chưa đăng nhập, chuyển hướng đến trang đăng nhập
                  
//             }
//       );
// }

// export default AuthComposition;
